# rifftrax

Rename helpers (run on the machine that has `\\amber\Rifftrax`):

- **Combined/** (letter-spaced names): `RUN_BUILD_RENAMER_MAPPING_FROM_LOG.bat` → `combined_rename_map_renamer_mapping*.csv`
- **Everything except Combined/**: `RUN_FILTER_MAPPING_REST.bat` → **`rest_of_repo_renamer_mapping*.csv`**. **Which CSV?** **`WHICH_CSV_FOR_RENAMER.txt`**. Troubleshooting: **`RENAMER_ZERO_MATCHES.txt`**.
